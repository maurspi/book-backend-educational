server:
  port: 8081  # El Backend 8080 para no chocar con el core.

spring:
  application:
    name: book-backend

# Configuración personalizada (Custom Properties)
book-core:
  url: http://localhost:8080